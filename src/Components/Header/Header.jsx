import { HeaderContainer, HeaderTitle, HeaderSubtitle } from './Header.styled';

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>EVA-I</HeaderTitle>
      <HeaderSubtitle>AI Assistant</HeaderSubtitle>
    </HeaderContainer>
  );
};
